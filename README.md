# c-programmiing

//create a switch case to display a 0 or x at the exact number of box which user has chosen

//create if statmeents FLAG==1 if any condition of thee rows columns or diagonal matches

//put flag arugment in for loop to stop looping once a player wins

//try to use dictionary to store all possible combinations
